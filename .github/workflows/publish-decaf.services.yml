name: Publish
on:
  workflow_call:
env:
  FULL_PACKAGE_VERSION: ${{ format('0.1.{0}+{1}', github.run_number, github.sha) }}
  PACKAGE_VERSION: ${{ format('0.1.{0}', github.run_number) }}
jobs:
  pack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Pack
        env:
          CSPROJ: src/extensions/decaf.services/decaf.services-nuget.csproj
        run: |
          dotnet add ${CSPROJ} package decaf-orm --version ${{ vars.LATEST_DECAF }}
          dotnet pack ${CSPROJ} -c Release -p:Version=${FULL_PACKAGE_VERSION} -o ./output
      - name: Upload Package
        uses: actions/upload-artifact@v2.2.4
        with:
          name: package
          path: ./output
          retention-days: 0

  publish:
    runs-on: ubuntu-latest
    needs: [pack]
    steps:
    - name: Download Artifact
      uses: actions/download-artifact@v4.1.7
      with:
        name: package
        path: ./output
    - name: Upload to NuGet
      env:
        NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        PACKAGE_NAME: ${{ format('decaf-orm.services.{0}.nupkg', env.PACKAGE_VERSION) }}
      run: |
        dotnet nuget push ./output/${PACKAGE_NAME} \
          -k ${NUGET_API_KEY} \
          -s https://api.nuget.org/v3/index.json
    - name: Persist Version
      env:
        GH_TOKEN: ${{ secrets.GH_CLI_PAT }}
      run: |
       gh variable set LATEST_SERVICES --body=${PACKAGE_VERSION} -R daniel-buchanan/decaf-orm